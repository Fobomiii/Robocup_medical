# 工程资源占用表

MCU：**STM32H743IIT6**（LQFP176）  
工程路径：`Program/`

---

## 1. 时钟资源

| 时钟域 | 频率 | 用途 |
|--------|------|------|
| SYSCLK / CPU | 400 MHz | 内核 |
| AHB / HCLK | 200 MHz | 总线 |
| APB1 / APB2 | 100 MHz | 外设 |
| FDCAN | 100 MHz | CAN |
| Timer 内核时钟（APB×2） | 200 MHz | TIM3 等 |
| HSE | 25 MHz | 外部晶振 |

PLL1：DIVM=5，DIVN=160，DIVP→400 MHz，DIVQ=8→100 MHz（FDCAN 等）。

---

## 2. GPIO / 引脚分配

### 2.1 已接入驱动（固件已实现）

| 引脚 | 信号 | 模块 |
|------|------|------|
| PH0 / PH1 | OSC_IN / OUT | HSE 25 MHz |
| PB8 | FDCAN1_RX | 底盘/臂 CAN |
| PB9 | FDCAN1_TX | 底盘/臂 CAN |
| PA9 | USART1_TX | HWT101CT |
| PA10 | USART1_RX | HWT101CT |
| PC10 | USART3_TX | OPS 全场定位 |
| PC11 | USART3_RX | OPS |
| PD1 | UART4_TX | MAX30102 |
| PD0 | UART4_RX | MAX30102 |
| PE8 | UART7_TX | GM65 二维码 |
| PE7 | UART7_RX | GM65 |
| PE1 | UART8_TX | GY-614 额温 |
| PE0 | UART8_RX | GY-614 |
| PA6 | TIM3_CH1 | 舵机 1（SG90） |
| PA7 | TIM3_CH2 | 舵机 2（SG90） |

### 2.2 CubeMX 已配置（`Program.ioc`，待生成代码/驱动）

| 引脚 | 信号 | 规划用途 |
|------|------|----------|
| PB11 | GPIO_Input（上拉） | 按钮 A（BTN_A） |
| PB10 | GPIO_Input（上拉） | 按钮 B（BTN_B） |
| PH8 | GPIO_Input（上拉） | 按钮 C（BTN_C） |
| PB12 | FDCAN2_RX | FDCAN2 保留 |
| PB13 | FDCAN2_TX | FDCAN2 保留 |
| PD5 | USART2_TX | RDK X5 通讯 |
| PD6 | USART2_RX | RDK X5 通讯 |
| PC6 | USART6_TX | STP23L A |
| PC7 | USART6_RX | STP23L A |
| PC12 | UART5_TX | STP23L B |
| PD2 | UART5_RX | STP23L B |
| PB6 | LPUART1_TX | STP23L C |
| PB7 | LPUART1_RX | STP23L C |
| PH4 | I2C2_SCL | OLED（SSD1309） |
| PH5 | I2C2_SDA | OLED |
| PE9 | GPIO_Output | 蜂鸣器（BUZZER，默认低电平关） |

### 2.3 规划但未写入 CubeMX

（当前无）

---

## 3. 串口资源

| 串口 | 引脚 TX/RX | 波特率 | 设备 | 收数方式 | 状态 |
|------|------------|--------|------|----------|------|
| USART1 | PA9 / PA10 | **115200** | HWT101CT | DMA1 Stream0 + IDLE | 已驱动 |
| USART2 | PD5 / PD6 | 115200 | RDK X5 | 待实现（建议中断/DMA） | ioc 已配 |
| USART3 | PC10 / PC11 | 115200 | OPS | 字节中断 | 已驱动 |
| UART4 | PD1 / PD0 | 57600 | MAX30102 | 字节中断 + 主动查询 | 已驱动 |
| UART5 | PC12 / PD2 | 115200 | STP23L B | 待实现 | ioc 已配 |
| UART7 | PE8 / PE7 | 9600 | GM65 | DMA1 Stream1 + IDLE | 已驱动 |
| UART8 | PE1 / PE0 | 9600 | GY-614 | 字节中断拼帧 | 已驱动 |
| USART6 | PC6 / PC7 | 115200 | STP23L A | 待实现 | ioc 已配 |
| LPUART1 | PB6 / PB7 | 115200 | STP23L C | 待实现（HAL_LPUART） | ioc 已配 |

**调试变量（HWT101）**：`hwt_zangle`、`hwt_online`（与 OPS 宏 `zangle` 不同）。

---

## 4. CAN / 定时器 / DMA

### FDCAN1（电机，已用）

| 项 | 值 |
|----|-----|
| 引脚 | PB8 / PB9 |
| 格式 | Classic CAN，1 Mbps |
| 过滤器 | StdFiltersNbr=1，接收 0x201–0x208 |
| 用途 | M3508×4（ID 1–4）+ M2006×1（ID 5） |

### FDCAN2（保留，ioc 已配）

| 项 | 值 |
|----|-----|
| 引脚 | PB12 / PB13 |
| 格式 | Classic CAN，1 Mbps（与 FDCAN1 同参） |
| 用途 | 预留；RDK 当前规划走 **USART2**，非 CAN |

### 定时器

| 定时器 | 用途 | 备注 |
|--------|------|------|
| TIM2 | HAL 系统 Tick | CubeMX 时间基 |
| TIM3 | PWM 舵机 CH1/CH2 | PSC=199，ARR=19999→50 Hz；**`Servo.c` 内 `tim3_hw_init()` 显式开时钟/配脚**（勿依赖被链接器删掉的 MSP） |

### DMA1

| Stream | 请求 | 模式 | 用途 |
|--------|------|------|------|
| Stream0 | USART1_RX | NORMAL | HWT101 |
| Stream1 | UART7_RX | NORMAL | GM65 |

### I2C2（OLED，ioc 已配）

| 项 | 值 |
|----|-----|
| 引脚 | PH4 SCL / PH5 SDA |
| 模式 | 开漏 + 上拉，AF4 |
| 速率 | 400 kHz（Timing `0x00901954`，I2C 内核时钟 100 MHz） |
| 用途 | OLED 显示屏（SSD1309 128×64）；驱动 `OLED_SSD1309` |
| 任务 | `oledTask` 占位，Generate Code 后可在该任务内刷新显示 |

---

## 5. NVIC 中断（应用相关）

| 中断 | 抢占优先级 | 用途 |
|------|------------|------|
| FDCAN1_IT0 | 5 | 电机 CAN |
| FDCAN2_IT0 | 5 | FDCAN2（ioc 已开，驱动未写） |
| USART1 / USART2 / USART3 / USART6 | 5 | 串口 + 部分 DMA |
| UART4 / UART5 / UART7 / UART8 | 5 | 串口 |
| LPUART1 | 5 | STP23L C |
| DMA1 Stream0/1 | 5 | HWT / GM65 RX |
| TIM2 | 15 | SysTick 替代时基 |
| PendSV / SysTick | 15 | FreeRTOS |

优先级组：NVIC_PRIORITYGROUP_4。

---

## 6. FreeRTOS 资源

| 配置项 | 值 |
|--------|-----|
| Tick | 1000 Hz |
| Heap | 65536 字节 |
| Minimal stack | 128 words |

| 任务 | CMSIS 优先级 | 栈（字节） | 职责 |
|------|--------------|------------|------|
| chassisTask | High (40) | 4096 | 底盘 1 kHz |
| armTask | AboveNormal (32) | 2048 | 臂角度 |
| defaultTask | Normal (24) | 1024 | MAX30102 1 Hz（测试时可能注释） |
| OPSUartTask | Normal (24) | 4096 | OPS 维护 |
| canHostTask | Normal (24) | 2048 | 占位 |
| oledTask | BelowNormal (16) | 2048 | SSD1309 显示 |

---

## 7. 驱动模块 ↔ 外设

| 源文件 | 外设 | 主要 API |
|--------|------|----------|
| `DJIMotorCtrlSTM32.cpp` | FDCAN1 | 底盘/臂电机控制 |
| `OPS.c` | USART3 | `pos_x/y/zangle`、`OPS_*`；**UART MSP 集中在此**（含 USART1 DMA 等） |
| `HWT101CT.c` | USART1 + DMA | `HWT101_GetYaw`、`hwt_zangle`；上电 `HWT101_CaliYaw()` |
| `GM65.c` | UART7 + DMA | `GM65_GetLastCode` |
| `MAX30102.c` | UART4 | `getBPM` / `getBPMRaw` |
| `GY614.c` | UART8 | `getTemp()` |
| `Servo.c` | TIM3 | `SERVO1_ANGLE` / `SERVO2_ANGLE` |

**待开发**：按钮（PB10/11/PH8）、RDK USART2、STP23×3、FDCAN2、**蜂鸣器驱动（PE9 已在 ioc 配置）**。OLED 已接 `OLED_SSD1309`。

---

## 8. 系统架构（感知与通讯）

```
Mid-360 ──────────────► RDK X5（点云/避障/相机二维码）
STP23L ×3 ──UART──► STM32（近距测距，ioc 已配引脚）
OPS / HWT101 ───────► STM32（场内定位 + 机体航向）

STM32 ◄──USART2 PD5/6──► RDK X5（障碍物摘要、导航指令等，UART 非 CAN）

FDCAN1 PB8/9 ──► DJI 电机
FDCAN2 PB12/13 ── 保留（当前 RDK 不走此路）
```

---

## 9. 资源余量小结

| 资源 | 已用 | 备注 |
|------|------|------|
| 串口 | 9 路规划（5 路已驱动） | 无空闲 USART 实例；新增需改规划 |
| I2C | I2C2（PH4/5）→ SSD1309 | 已驱动 |
| FDCAN | FDCAN1 + FDCAN2 占脚 | FDCAN2 可暂不初始化 |
| 定时器 | TIM2 + TIM3 | TIM3 舵机；蜂鸣器用 PE9 纯 GPIO |
| DMA Stream | 2/8（DMA1） | USART2 可再加 RX DMA |
| GPIO | 按钮 3 + 蜂鸣器 1 | ioc 已配，驱动待写 |

---

## 10. 接线速查

- **TTL 模块**：MCU TX→模块 RX，MCU RX→模块 TX，GND 共地。
- **CAN**：PB8/PB9 电机；PB12/PB13 预留 FDCAN2。
- **舵机**：信号 PA6/PA7；5V 独立供电，GND 共地。
- **按钮**：上拉输入，按下接 GND（PB11=A，PB10=B，PH8=C）。
- **OLED（I2C2）**：PH4=SCL，PH5=SDA，3.3 V + GND；模块常带板上拉，长线可酌情加强。
- **蜂鸣器（PE9）**：高电平响/低电平静音（有源蜂鸣器）；无源蜂鸣器需后续 PWM 驱动。
- **波特率**：MAX30102 **57600**；GM65/GY-614 **9600**；OPS/HWT/RDK/STP23 **115200**（HWT 模块需与 MCU 一致）。

---

## 11. 已知问题与修复记录

| 问题 | 处理 |
|------|------|
| 舵机无 PWM，`TIM3` 寄存器全 0 | `HAL_TIM_MspInit` 在 `Servo.c` 中被链接器删除 → **`MX_TIM3_Init()` 前增加 `tim3_hw_init()`** |
| `Servo.c` `TIM_Channel` 编译错误 | 改为 `uint32_t` |
| OPS `zangle` 与 HWT 航向 | HWT 调试用 **`hwt_zangle`**，勿与 OPS 宏 `zangle` 混用 |


负责人：方铂洲，白世凡
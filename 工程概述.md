# RoboCup 医疗机器人工程概述

## 1. 工程定位

本工程用于 RoboCup 医疗救援场景，基于 **STM32H743IIT6** 实现移动底盘、机械臂、传感器与执行器的统一控制。采用 **CubeMX + Keil MDK-ARM + FreeRTOS (CMSIS-RTOS v2)** 组织应用层，代码在 `Program/Core`。

## 2. 硬件与时钟

| 项 | 参数 |
|----|------|
| MCU | STM32H743IIT6 |
| 外部晶振 | HSE 25 MHz |
| 主频 | SYSCLK **400 MHz**（PLL：DIVM1=5，DIVN1=160，DIVP=2） |
| FDCAN 时钟 | **100 MHz**（H743 FDCAN 上限） |
| I2C 时钟 | I2C123 **100 MHz** |
| 时基 | TIM2（HAL Tick） |

## 3. 软件模块总览

| 模块 | 接口 | 主要 API / 用法 |
|------|------|-----------------|
| **底盘电机** | 4× M3508+P19、6 寸全向轮，车体半径 22.5 cm、旋转等效半径 25 cm，CAN ID 1–4 | `DJI_Chassis_SetVelocityCommand` / `DJI_Motor_ChassisTask`（内环 1kHz） |
| **Nav2 桥接** | UART8，115200 8N1 | OPS9/HWT101CT 位姿 50 Hz 上报、`/cmd_vel_safe` 接收、任务态门控、250 ms 超时停车 |
| **医疗任务** | GM65 + 双舵机 + Nav2 目标 | `MedicalTask_Update`：护士台扫码、床位顺序、放药和返航 |
| **旧底盘外环** | 历史参考 | `ChassisCtrl` / `Navigation` / `PathFollower` 保留源码但不进入实时控制链 |
| **机械臂电机** | 1× M2006+P36，CAN ID 5 | `DJI_Arm_CtrlAngle` / `DJI_Arm_CtrlAngleTimed` |
| **OPS 全场定位** | USART3 定位帧 | `pos_x/pos_y/zangle`，`OPS_GetX/Y/Yaw` |
| **HWT101CT** | Z 轴航向，维京 TTL | `HWT101_GetYaw` / `hwt_zangle` |
| **GM65** | 二维码扫描 | `GM65_GetLastCode` / `GM65_FrameReady` |
| **MAX30102** | 心率，模块 AT | `getBPM` / `getBPMRaw`，`MAX30102_Update` @1Hz |
| **GY-614** | 红外额温 | `getTemp()`（BO/100 ℃） |
| **舵机** | 2× SG90 PWM | `SERVO1_ANGLE` / `SERVO2_ANGLE` |
| **OLED** | I2C2 PH4/PH5 | SSD1309 128×64，`OLED_SSD1309`，`oledTask` 刷新 |
| **蜂鸣器** | PE9 GPIO 输出 | ioc 已配（`BUZZER`，默认低）；**驱动待写** |

当前底盘由 Nav2 生成 ROS 车体速度，STM32 的 `DJIMotorCtrlSTM32` 完成全向轮解算和
3508 速度内环（1 kHz）。旧 `ChassisCtrl` 位姿外环仍在工程中用于历史参考，但链接器会
移除其更新函数，实际定位只采用 OPS9 XY 和 HWT101CT 航向。

## 4. 工程结构

```
Program/
  Program.ioc              # CubeMX 配置（含 I2C2 OLED）
  Core/Inc|Src/            # 应用与驱动
    DJIMotorCtrlSTM32.*    # 大疆电机 C++（内环）
    ChassisCtrl.*          # 底盘外环位姿（C）
    PID.cpp / PID.h        # 三轴位姿 PID（C++ 实现，C 接口）
    OPS.*                  # 全场定位
    HWT101CT.* / GM65.* / MAX30102.* / GY614.* / Servo.*
    main.c                 # 初始化 + FreeRTOS 任务入口
  MDK-ARM/                 # Keil 工程
  Drivers/                 # HAL / CMSIS
```

## 5. FreeRTOS 任务

| 任务 | 优先级 | 栈 | 职责 |
|------|--------|-----|------|
| chassisTask | High | 4KB | 底盘电机内环 1 kHz（`DJI_Motor_ChassisTask`） |
| armTask | AboveNormal | 2KB | 臂角度控制 |
| defaultTask | Normal | 1KB | 兼容障碍帧监视，不参与 Nav2 控制 |
| OPSUartTask | Normal | 4KB | OPS 维护 |
| canHostTask | Normal | 2KB | `MedicalTask_Update`、位姿上报；仅导航态+`FOLLOWING` 执行 Nav2 速度 @100Hz |
| oledTask | BelowNormal | 2KB | SSD1309 显示 |

Heap：`configTOTAL_HEAP_SIZE = 64 KB`，Tick 1 kHz。

## 6. 通讯与外设摘要

- **FDCAN1**：底盘 CAN，1 Mbps，差分线 PB8/PB9
- **FDCAN2**：PB12/PB13 保留（RDK 走 USART2）
- **串口**：USART1/3，UART4/7/8 等，详见 `工程资源占用.md`
- **I2C2**：PH4 SCL / PH5 SDA → OLED（400 kHz）
- **蜂鸣器**：PE9 GPIO 输出（`BUZZER`，默认关）
- **TIM3**：50 Hz PWM 舵机 PA6/PA7
- **DMA**：USART1_RX、UART7_RX（HWT / GM65 用 DMA+IDLE）

## 7. 典型调用关系

```
main()
  → 外设 Init（FDCAN / UART / I2C2 / Servo / 各传感器）
  → osKernelStart()
      chassisTask  → DJI_Motor_ChassisTask()     // 内环 1kHz
      armTask      → DJI_Motor_ArmStart()
      OPSUartTask  → OPS_Task()
      defaultTask  → 兼容障碍帧监视
      canHostTask  → MedicalTask_Update + NUC_Nav_Service @100Hz
                   → /cmd_vel_safe → DJI_Chassis_SetVelocityCommand
      oledTask     → ssd1309_*（SSD1309）
  Nav2 定位：OPS9 XY + HWT101CT yaw → stm32_bridge → /odom 与 TF
  运动闭环：Nav2/Collision Monitor → NUC/STM32 双层任务门控 → UART8 → 四轮速度内环
  其他任务读实时量：getBPM / getTemp / HWT101_GetYaw / GM65_GetLastCode
```

## 8. 资料与参考目录（非主工程）

- `MAX30102模块资料/` — 心率模块 AT 命令
- `GY614615V3资料/` — 额温计 UART/I2C 说明
- Arduino `DJIMotorCtrlESP`、旧工程 `RC_Old` — 底盘与 OPS 参考

## 9. 备注

- HWT / GM65：DMA + IDLE 组帧；GY614 / MAX30102 / OPS：中断或查询式串口。
- MAX30102 滤波：`|Δ|>3` 才更新，NULL 窗口。
- GY614：`getTemp()` = 文档包头后公式 `(H×256+L)/100`。
- `canHostTask` 已切换到 Nav2；旧 `Nav_Update/FSM_Update/OA_Update` 不再调用。
- 详细引脚表见 **`工程资源占用.md`**。
